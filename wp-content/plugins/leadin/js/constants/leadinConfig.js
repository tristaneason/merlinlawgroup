const {
  adminUrl,
  ajaxUrl,
  env,
  hubspotBaseUrl,
  leadinPluginVersion,
  plugins,
  phpVersion,
  portalId,
  theme,
  wpVersion,
} = window.leadinConfig;

const i18n = window.leadinI18n;

export {
  adminUrl,
  ajaxUrl,
  env,
  hubspotBaseUrl,
  i18n,
  leadinPluginVersion,
  plugins,
  phpVersion,
  portalId,
  theme,
  wpVersion,
};
